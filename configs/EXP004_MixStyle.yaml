# EXP004: ResNet50 + MixStyle Domain Generalization
name: "EXP004_MixStyle"
backbone: "resnet50"
pretrained: true

feature_module: "identity"
attention: "none"
domain_generalization: "mixstyle"
mixstyle:
  mix_prob: 0.5
  alpha: 0.1

head: "softmax"
num_classes: 4
dropout: 0.5
